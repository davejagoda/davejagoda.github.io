---
title: Useful MongoDB commands
layout: appleII
---

from command line
-----------------

`mongo`

`mongo mongodb://<username>:<password>@<hostname>/<dbname>`

from mongo shell
----------------

`show dbs`

`use <dbname>`

`show collections`

`db.<collectionname>.find()`

`quit()`
